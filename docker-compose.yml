services:
  bounce:
    build: .
    ports:
      - "3001:3001"
    environment:
      - BOUNCE_PORT=3001
    volumes:
      - bounce-data:/data
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:3001/health"]
      interval: 30s
      timeout: 3s
      retries: 3
      start_period: 5s

volumes:
  bounce-data:
